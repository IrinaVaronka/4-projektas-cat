{
  "name": "4-projektas-cat",
  "version": "1.0.0",
  "description": "4-projektas-cat",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --host=localhost --port=4204",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cat"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
